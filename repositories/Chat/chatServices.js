
const pool = require("../../config/dbConfig");
const messageModel = require("../../model/messages");
const userModel = require("../../model/userModel");

class dbchatServices{

    async getChat(users){
        
    }
}